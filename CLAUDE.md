# Docker & Kubernetes learning workspace

This folder (`/Users/mutyalaqwipo/qwipo/docker`) is the user's dedicated
workspace for **learning and discussing Docker and Kubernetes**. Work tied to
the session named **"docker"**.

## Context
- The user is learning Docker & Kubernetes hands-on (beginner-friendly pace —
  explain clearly, give copy-pasteable terminal commands, avoid jargon).
- Reference doc in this folder: **`docker-kubernetes-notes.md`** — the full
  step-by-step record of building the `meety-events` Angular image, running it
  as a container, and deploying it to Kubernetes, plus the two mistakes made
  and their fixes.
- The practice app is **meety-events** (Angular 21) at
  `/Users/mutyalaqwipo/qwipo/infinit-events` — that's where the actual
  `Dockerfile`, `.dockerignore`, and `k8s/` manifests live.

## How to help here
- Default to Docker / Kubernetes topics.
- Keep explanations concise and practical; show the command, then a one-line
  "what it does".
- When the user hits an error, diagnose the cause plainly and give the fix.
